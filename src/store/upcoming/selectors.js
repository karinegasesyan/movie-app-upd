export const getError = (state) => {
    return state. upcoming_movies.error;
};

export const getLoading = (state) => {
    return state. upcoming_movies.loading
};

export const getUpcomingMovies = (state) => {
    return state.upcoming_movies.upcomingMovies
};